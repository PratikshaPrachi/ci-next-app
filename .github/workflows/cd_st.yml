name: Deploy to staging

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code
      - uses: actions/checkout@v2

      # Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'

      # Install pnpm
      - name: Install pnpm
        run: npm install -g pnpm

      # Set up SSH
      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          echo "${{ secrets.KNOWN_HOSTS }}" > ~/.ssh/known_hosts

      # Deploy to staging server
      - name: Deploy to Staging Server
        run: |
          ssh -i ~/.ssh/id_rsa ubuntu@18.212.54.148 -o StrictHostKeyChecking=no -t "
            cd ci-next-app &&
            git pull origin main &&
            pnpm install &&
            pnpm run build &&
            pm2 restart fe &&
            pm2 restart http &&
            pm2 restart ws
          "

      